[tool.uv.workspace]
members = ["source/decor8rd", "source/decor8rctl", "source/decor8", "share/decor8rlib"]

[dependency-groups]
dev = [
    "pytest>=8.3.4",
    "ruff>=0.8.4",
]
common = [
    "loguru>=0.7.3",
    "pyyaml>=6.0",
]
